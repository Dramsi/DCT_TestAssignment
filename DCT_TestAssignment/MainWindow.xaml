<Window x:Class="DCT_TestAssignment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Digital Cloud Technologies" Height="450" Width="400">
    <DockPanel>
        <Label Name="label1" DockPanel.Dock="Top" Margin="20,0,0,0">Search by name:</Label>
        <TextBox DockPanel.Dock="Top" Margin="20,0,200,0" Name="searchFilter" TextChanged="searchFilter_TextChanged" />
        <ListView Margin="20" Name="coinsList" DockPanel.Dock="Left" MouseDoubleClick="coinsList_MouseDoubleClick" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="100" DisplayMemberBinding="{Binding name}" />
                    <GridViewColumn Header="Symbol" Width="50" DisplayMemberBinding="{Binding symbol}" />
                    <GridViewColumn Header="Supply" Width="150" DisplayMemberBinding="{Binding supply}" />
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</Window>
